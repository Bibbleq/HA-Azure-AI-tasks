{
  "config": {
    "step": {
      "user": {
        "title": "Set up Azure AI Tasks",
        "description": "Enter your Azure AI endpoint, API key, and model(s). At least one model must be configured.",
        "data": {
          "name": "Name",
          "endpoint": "Azure AI Endpoint URL",
          "api_key": "API Key",
          "chat_model": "Chat Model (optional)",
          "image_model": "Image Model (optional)",
          "use_responses_api": "Use Responses API (v1) for chat tasks",
          "enable_web_search": "Enable web search (uses Bing grounding; requires the Responses API and incurs additional costs)"
        }
      },
      "reconfigure": {
        "title": "Reconfigure Azure AI Tasks",
        "description": "Update your Azure AI endpoint, API key, and model(s). At least one model must be configured.",
        "data": {
          "name": "Name",
          "endpoint": "Azure AI Endpoint URL",
          "api_key": "API Key",
          "chat_model": "Chat Model (optional)",
          "image_model": "Image Model (optional)",
          "use_responses_api": "Use Responses API (v1) for chat tasks",
          "enable_web_search": "Enable web search (uses Bing grounding; requires the Responses API and incurs additional costs)"
        }
      }
    },
    "error": {
      "unknown": "Unexpected error occurred",
      "cannot_connect": "Failed to connect - check the endpoint URL and API key",
      "no_models_configured": "At least one model (chat or image) must be configured"
    },
    "abort": {
      "already_configured": "Device is already configured",
      "reconfigure_successful": "Reconfiguration was successful"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Azure AI Tasks Options",
        "description": "Configure your preferred models. At least one model must be configured.",
        "data": {
          "chat_model": "Chat Model (optional)",
          "image_model": "Image Model (optional)",
          "use_responses_api": "Use Responses API (v1) for chat tasks",
          "enable_web_search": "Enable web search (uses Bing grounding; requires the Responses API and incurs additional costs)"
        }
      }
    },
    "error": {
      "no_models_configured": "At least one model (chat or image) must be configured"
    }
  }
}
